services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: myeconomy-backend
    ports:
      - "5050:5000"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - DB_CONNECTION_STRING=Server=host.docker.internal,1433;Database=MyEconomy;User Id=sa;Password=Olakase2.;TrustServerCertificate=True
      - FRONTEND_URL=
    extra_hosts:
      - "host.docker.internal:host-gateway"
